server:
  port: 8083
spring:
  datasource:
    url: jdbc:h2:mem:restaurant-order
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-console
axon:
  snapshot:
    trigger:
      treshold:
        order: 100
